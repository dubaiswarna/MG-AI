═══════════════════════════════════════════════════════════════════
    MYSQL INSTALLATION GUIDE - COMPLETE STEP-BY-STEP
═══════════════════════════════════════════════════════════════════

YOU ARE HERE: MySQL is downloading...

═══════════════════════════════════════════════════════════════════
STEP 1: CHOOSE SETUP TYPE
═══════════════════════════════════════════════════════════════════

✅ SELECT: "Full"

Why Full?
  - MySQL Server (database engine)
  - MySQL Workbench (visual manager - ESSENTIAL!)
  - MySQL Shell (command line)
  - All connectors and tools
  - Perfect for production use
  - Handles huge data volumes

Click "Next"

═══════════════════════════════════════════════════════════════════
STEP 2: CHECK REQUIREMENTS
═══════════════════════════════════════════════════════════════════

If any requirements missing:
  - Click "Execute" to install them
  - Wait for completion
  - Click "Next"

═══════════════════════════════════════════════════════════════════
STEP 3: INSTALLATION
═══════════════════════════════════════════════════════════════════

Click "Execute"
Wait 10-15 minutes
When all components show green checkmarks:
  - Click "Next"

═══════════════════════════════════════════════════════════════════
STEP 4: PRODUCT CONFIGURATION - TYPE AND NETWORKING
═══════════════════════════════════════════════════════════════════

Config Type: Development Computer ✅
Connectivity: TCP/IP ✅
Port: 3306 ✅
Open Windows Firewall for network access: Yes ✅
Show Advanced and Logging Options: No

Click "Next"

═══════════════════════════════════════════════════════════════════
STEP 5: AUTHENTICATION METHOD
═══════════════════════════════════════════════════════════════════

✅ SELECT: "Use Strong Password Encryption (RECOMMENDED)"

This is more secure!

Click "Next"

═══════════════════════════════════════════════════════════════════
STEP 6: ACCOUNTS AND ROLES (MOST IMPORTANT!)
═══════════════════════════════════════════════════════════════════

ROOT PASSWORD:
  Password: TradingDB@2025!Secure
  Confirm:  TradingDB@2025!Secure

📝 WRITE THIS DOWN IN A SAFE PLACE! 📝

MySQL User Accounts: (Skip for now, we'll create later)

Click "Next"

═══════════════════════════════════════════════════════════════════
STEP 7: WINDOWS SERVICE
═══════════════════════════════════════════════════════════════════

Configure MySQL Server as a Windows Service: Yes ✅
Windows Service Name: MySQL80 ✅
Start the MySQL Server at System Startup: Yes ✅
Run Windows Service as: Standard System Account ✅

Click "Next"

═══════════════════════════════════════════════════════════════════
STEP 8: SERVER FILE PERMISSIONS
═══════════════════════════════════════════════════════════════════

Keep default settings ✅

Click "Next"

═══════════════════════════════════════════════════════════════════
STEP 9: APPLY CONFIGURATION
═══════════════════════════════════════════════════════════════════

Click "Execute"

Wait for all configuration steps to complete (green checkmarks)

Click "Finish"

═══════════════════════════════════════════════════════════════════
STEP 10: COMPLETE INSTALLATION
═══════════════════════════════════════════════════════════════════

May configure other products (Workbench, etc.)
  - Click through with defaults
  - Click "Next" → "Finish"

═══════════════════════════════════════════════════════════════════
VERIFICATION: IS MYSQL RUNNING?
═══════════════════════════════════════════════════════════════════

Open PowerShell:

  Get-Service MySQL80

Should show:
  Status: Running ✅

If not running:
  Start-Service MySQL80

═══════════════════════════════════════════════════════════════════
AFTER INSTALLATION: CREATE DATABASE
═══════════════════════════════════════════════════════════════════

METHOD 1: MYSQL WORKBENCH (VISUAL - EASIEST)
───────────────────────────────────────────────

1. Open MySQL Workbench (from Start menu)

2. Click "Local instance MySQL80"

3. Enter password: TradingDB@2025!Secure

4. CREATE DATABASE:
   - Click "Database" menu → "Create Schema"
   - Schema Name: trading_platform_db
   - Charset: utf8mb4
   - Collation: utf8mb4_unicode_ci
   - Click "Apply" → "Apply" → "Finish"

5. CREATE USER:
   - Click "Server" → "Users and Privileges"
   - Click "Add Account" button
   
   Login Tab:
     Login Name: trading_user
     Authentication Type: Standard
     Limit to Hosts Matching: localhost
     Password: Trading@2025!Secure
     Confirm: Trading@2025!Secure
   
   Schema Privileges Tab:
     - Select "trading_platform_db" from dropdown
     - Click "Select ALL" button
     - Click "Apply"
   
   Click "Close"

6. VERIFY:
   - Go to "Schemas" tab (left side)
   - You should see: trading_platform_db
   - Expand it - it's empty (tables will be created by Prisma)

METHOD 2: COMMAND LINE (FOR PROS)
──────────────────────────────────

PowerShell:

  mysql -u root -p
  # Enter password: TradingDB@2025!Secure

Then run:

  CREATE DATABASE trading_platform_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
  CREATE USER 'trading_user'@'localhost' IDENTIFIED BY 'Trading@2025!Secure';
  GRANT ALL PRIVILEGES ON trading_platform_db.* TO 'trading_user'@'localhost';
  FLUSH PRIVILEGES;
  SHOW DATABASES;
  EXIT;

═══════════════════════════════════════════════════════════════════
AFTER DATABASE CREATED: UPDATE PROJECT
═══════════════════════════════════════════════════════════════════

1. Edit environment file:

  notepad "C:\python\MG AI\ai-trading-platform\.env.local"

2. Change DATABASE_URL line to:

  DATABASE_URL="mysql://trading_user:Trading@2025!Secure@localhost:3306/trading_platform_db"

3. Save and close

4. Generate Prisma client:

  cd "C:\python\MG AI\ai-trading-platform"
  npx prisma generate

5. Create all tables:

  npx prisma db push

6. Verify database (optional):

  npx prisma studio
  # Opens http://localhost:5555
  # You'll see all 8 tables!

7. Start platform:

  npm run dev

8. Open browser:

  http://localhost:3000

═══════════════════════════════════════════════════════════════════
PASSWORDS TO REMEMBER
═══════════════════════════════════════════════════════════════════

MySQL Root:
  Username: root
  Password: TradingDB@2025!Secure

Trading Database User:
  Username: trading_user
  Password: Trading@2025!Secure
  Database: trading_platform_db

📝 SAVE THESE SOMEWHERE SAFE! 📝

═══════════════════════════════════════════════════════════════════
TABLES THAT WILL BE CREATED
═══════════════════════════════════════════════════════════════════

1. users             (User accounts & authentication)
2. portfolios        (User portfolios & capital)
3. positions         (Active trading positions)
4. signals           (AI-generated signals)
5. trade_history     (Closed trades)
6. stock_data        (Stock price cache - HUGE DATA!)
7. alerts            (Notifications)
8. system_settings   (Configuration)

Total: 8 tables, optimized for millions of rows!

═══════════════════════════════════════════════════════════════════
TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════

MySQL service won't start:
  - Open Services (services.msc)
  - Find MySQL80
  - Right-click → Start

Can't connect to MySQL:
  - Check password is correct
  - Check MySQL service is running
  - Check port 3306 is not blocked

Prisma can't connect:
  - Verify DATABASE_URL is correct
  - Check user has privileges
  - Test: mysql -u trading_user -p trading_platform_db

═══════════════════════════════════════════════════════════════════
READY!
═══════════════════════════════════════════════════════════════════

When you see this message in your browser:

  "MG AI Trading Platform" 
  Beautiful homepage with features

✅ YOU'RE LIVE!

═══════════════════════════════════════════════════════════════════

Follow this guide step-by-step after MySQL download completes!

